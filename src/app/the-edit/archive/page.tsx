import CollectionListing from "../../../components/collections/CollectionListing";

export default function ArchivePage() {
  return (
    <CollectionListing
      eyebrow="The Edit · Archive"
      title="Archive"
      blurb="Pieces from past seasons, kept a while longer. Made in considered quantities the first time, so what remains here is limited — the last of a cut once it is gone is gone."
      collectionSlug="archive"
    />
  );
}
